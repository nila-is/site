---
import Layout from './Layout.astro';

interface Props {
  frontmatter: {
    title: string;
    pubDate: string;
    description: string;
  };
}

const { frontmatter } = Astro.props;
---

<Layout 
  title={frontmatter.title}
  description={frontmatter.description}
  article={true}
>
  <div>
    <h1 class="text-xl font-bold mb-2">{frontmatter.title}</h1>
    <time class="text-gray-600 text-xs block mb-8">
      {new Date(frontmatter.pubDate).toLocaleDateString('en-US', {
        year: 'numeric',
        month: 'long',
        day: 'numeric'
      })}
    </time>
    
    <div class="space-y-4 text-xs leading-relaxed">
      <slot />
    </div>
  </div>

  <div class="mt-12 pt-8 border-t">
    <a href="/blog" class="text-blue-500 hover:underline text-xs">← Back to all posts</a>
  </div>
</Layout>
