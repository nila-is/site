---
interface Props {
  href: string;
}

const { href } = Astro.props;
---

<a href={href} target="_blank" rel="noopener noreferrer"><slot /><span class="inline-block ml-0.5 text-[0.7em] opacity-60">↗</span></a>

