---
import FormattedDate from './FormattedDate.astro';

interface Props {
  title: string;
  url: string;
  pubDate: string;
  description: string;
}

const { title, url, pubDate, description } = Astro.props;
---

<article>
  <h2 class="text-lg mb-2">
    <a href={url} class="hover:underline">
      {title}
    </a>
  </h2>
  <FormattedDate date={pubDate} class="text-slate-600 block mb-2 text-xs" />
  <p class="text-xs text-slate-800">{description}</p>
</article>

