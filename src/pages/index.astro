---
import Layout from '../layouts/Layout.astro';
import TestimonialList from '../components/TestimonialList.astro';
import LabeledListItem from '../components/LabeledListItem.astro';
import Section from '../components/Section.astro';
import Prose from '../components/Prose.astro';
import ExternalLink from '../components/ExternalLink.astro';
import { Image } from 'astro:assets';
import headshot from '../assets/headshot.jpg';
import { siteConfig } from '../config/site';
import { engagements } from '../data/services';
---

<Layout title="Home">
  <h1 class="text-3xl md:text-4xl font-bold mb-6 leading-tight">
    Ship production AI in 30 days
  </h1>

  <Prose>
    <p>
      Most AI projects never make it to production. I help CTOs and technical
      leaders ship AI systems that actually work in your environment. I focus on
      practical implementations that your team can maintain and extend.
    </p>
    <p>
      Prior to consulting, I was an early engineer at two AI-driven companies
      that were acquired: <strong>Drift</strong> (conversational AI, acquired by
      Vista for $1.2B) and <strong>Kensho</strong> (AI for financial services, acquired
      by S&P Global for $550M).
    </p>
    <p>I help with:</p>
    <ul>
      <li>
        <strong>Product Integration</strong> — Add AI features your customers will
        actually use
      </li>
      <li>
        <strong>Team Enablement</strong> — Building up your team's capability to
        ship AI independently
      </li>
      <li>
        <strong>Workflow Automation</strong> — Replace manual processes that take
        hours with AI systems that deliver results in minutes
      </li>
    </ul>
  </Prose>

  <Section id="how-i-work" title="What I Offer">
    <dl class="space-y-8" role="list">
      {
        engagements.map((engagement) => (
          <div>
            <LabeledListItem
              label={engagement.label}
              description={engagement.description}
              pricing={engagement.pricing}
              duration={engagement.duration}
              link={engagement.link}
            />
            <div class="mt-6">
              <TestimonialList
                testimonials={engagement.testimonials}
                id={engagement.testimonialsId}
              />
            </div>
          </div>
        ))
      }
    </dl>
  </Section>

  <Section id="about" title="About">
    <div class="flex flex-col">
      <Image
        src={headshot}
        alt="Professional headshot of Vignesh Mohankumar"
        class="w-2/3 sm:w-1/2 md:w-1/3 mb-6 rounded-lg"
        width={400}
        height={400}
        loading="eager"
      />
      <Prose>
        <p>
          I'm based in New York, NY. I grew up in New England and was born near
          Chennai, India. Nila means moon in my native language Tamil (my sister
          suggested the name).
        </p>
        <p>
          Find me on <ExternalLink href={siteConfig.social.linkedin}
            >LinkedIn</ExternalLink
          >, <ExternalLink href={siteConfig.social.twitter}>X</ExternalLink> and
          my <ExternalLink href={siteConfig.social.personalBlog}
            >personal blog</ExternalLink
          >.
        </p>
      </Prose>
    </div>
  </Section>
</Layout>
