---
import Layout from '../layouts/Layout.astro';
import Testimonial from '../components/Testimonial.astro';
import TestimonialList from '../components/TestimonialList.astro';
import LabeledListItem from '../components/LabeledListItem.astro';
import Section from '../components/Section.astro';
import Prose from '../components/Prose.astro';
import Button from '../components/Button.astro';
import ExternalLink from '../components/ExternalLink.astro';
import { Image } from 'astro:assets';
import headshot from '../assets/headshot.jpg';
import { SITE_CONFIG } from '../config/site';
import { implementationTestimonials, advisoryTestimonials, trainingTestimonials } from '../data/testimonials';
import { services, engagementModels } from '../data/services';
---

<Layout title="Home">
  <h1 class="text-2xl font-bold mb-4">Ship production AI in 30 days</h1>

  <Prose>
    <p>
      I partner with technical leaders to design, build, and ship reliable AI systems to production.
    </p>
    <p>
      I help with <strong>Automation</strong> (replacing slow or costly processes with AI), <strong>Exploration</strong> (integrating AI capabilities into your platform), and <strong>Enablement</strong> (setting up tools, guardrails, and best practices so your team ships AI reliably).
    </p>
    <p>
      I've partnered with companies in industries including enterprise SaaS, consumer social, healthcare, fintech, and crypto.
    </p>
    <p>
      Prior to consulting, I was an early engineer at two AI-driven companies that were acquired: <strong>Drift</strong> (conversational AI, acquired by Vista for $1.2B) and <strong>Kensho</strong> (AI for financial services, acquired by S&P Global for $550M).
    </p>
    <p class="pt-1">
      <Button href="https://nila.fillout.com/t/mZZ8cxD4tNus" external>
        Have an idea? Reach out →
      </Button>
    </p>
  </Prose>

  <Section id="how-we-work" title="How We Work">
    <dl class="space-y-8">
      <div>
        <LabeledListItem 
          label={engagementModels[0].label}
          description={engagementModels[0].description}
        />
        <div class="mt-4 ml-6">
          <TestimonialList testimonials={implementationTestimonials} initialCount={2} id="implementation" />
        </div>
      </div>

      <div>
        <LabeledListItem 
          label={engagementModels[1].label}
          description={engagementModels[1].description}
        />
        <div class="mt-4 ml-6">
          <TestimonialList testimonials={advisoryTestimonials} initialCount={2} id="advisory" />
        </div>
      </div>

      <div>
        <LabeledListItem 
          label={engagementModels[2].label}
          description={engagementModels[2].description}
        />
        <div class="mt-4 ml-6">
          <Prose>
            <p class="text-sm">
              Learn more and join the next cohort: <ExternalLink href="https://maven.com/nila/ai-coding-accelerator" class="hover:text-blue-600 transition-colors">AI Coding Accelerator</ExternalLink>. I offer a generous discount when you send a team.
            </p>
          </Prose>
          <div class="mt-4">
            <TestimonialList testimonials={trainingTestimonials} initialCount={2} id="training" />
          </div>
        </div>
      </div>
    </dl>
  </Section>

  

  <Section id="about" title="About">
    <div class="flex flex-col">
      <Image 
        src={headshot} 
        alt="Professional headshot of Vignesh Mohankumar" 
        class="w-2/3 sm:w-1/2 md:w-1/3 mb-6 rounded-lg"
        width={400}
        height={400}
      />
      <Prose>
        <p>
          I'm based in New York, NY. I grew up in New England and was born near Chennai, India. Nila means moon in my native language Tamil (my sister suggested the name).
        </p>
        <p>
          Find me on 
          <ExternalLink href={SITE_CONFIG.social.linkedin} class="hover:text-blue-600 transition-colors">LinkedIn</ExternalLink>,
          <ExternalLink href={SITE_CONFIG.social.twitter} class="hover:text-blue-600 transition-colors">X</ExternalLink>
          and my <ExternalLink href={SITE_CONFIG.social.personalBlog} class="hover:text-blue-600 transition-colors">personal blog</ExternalLink>.
        </p>
      </Prose>
    </div>
  </Section>
</Layout>
